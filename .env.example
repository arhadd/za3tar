# za3tar API keys — copy to .env (gitignored). Keys are read by the Rust core,
# never exposed to the webview. Add each as its adapter lands (M2/M3).

# ASR engines (M2) — trial all, pick the best on real Dubai/Amman audio.
ELEVENLABS_API_KEY=
SPEECHMATICS_API_KEY=
GOOGLE_AI_API_KEY=

# Notes generation (M3).
ANTHROPIC_API_KEY=
